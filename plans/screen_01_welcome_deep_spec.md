# 📐 Максимально детальный спецификационный разбор: Экран 01 (Onboarding Welcome Screen)

Данный документ содержит ультра-точный технический разбор **Первой страницы (Экран онбординга `route_01_onboarding_welcome.dart`)** на основепрямой выгрузки дерева геометрии из файла Figma (Node ID `275:254`).

---

## 📱 1. Главный холст экрана (Main Screen Frame Node `275:269`)
* **Точные размеры:** `360.0px` (ширина) × `848.2px` (высота).
* **Фон экрана:** `#0A0E1A` (Solid 100%).
* **Базовые поля карточки (Content Width):** `312.0px` (боковые поля по `24.0px` слева и справа).

---

## 🔲 2. Верхний логотип-контейнер (Logo Box Node `275:276` & `275:283`)
* **Позиция от верха холста:** Y = `56.0px` (`184px - 128px`).
* **Внешние размеры контейнера:** `60.0px` × `60.0px`.
* **Геометрия и стиль контейнера:**
  * **Цвет фона:** `#10172F`.
  * **Скругление углов (Border Radius):** `16.0px`.
  * **Обводка (Stroke / Border):** `1.5px`, цвет `#2A3A6A`.
* **Вложенная иконка книги (`logoonboarding.png` Node `275:284`):**
  * **Размеры иконки:** `27.0px` × `27.0px`.
  * **Внутренний отступ (Padding):** `16.5px` со всех 4 сторон (центрирование внутри `60x60`).

---

## 🔤 3. Текст названия приложения (`LitScroll` Node `275:295`)
* **Позиция Y:** `131.0px` (`259px - 128px`).
* **Вертикальный отступ от контейнера логотипа:** `15.0px`.
* **Размер текстового блока:** `81.0px` × `24.0px`.
* **Параметры типографики:**
  * **Текст:** `LitScroll`
  * **Гарнитура (Font Family):** `Inter`
  * **Начертание (Font Weight):** `700` (Bold)
  * **Размер шрифта (Font Size):** `20.0px`
  * **Цвет текста:** `#E8ECFF`
  * **Точная высота строки (Line Height):** `24.2px`
  * **Выравнивание:** По центру

---

## 🎨 4. Центральная иллюстрация (`Groupslide.png` Node `275:298` / `338:2`)
* **Позиция Y:** `184.0px` (`312px - 128px`).
* **Вертикальный отступ от названия `LitScroll`:** `29.0px`.
* **Размер иллюстрации:** `280.0px` × `280.0px`.
* **Состав векторных слоев внутри иллюстрации:**
  1. **Фоновый круг-свечение:** Диаметр `280px`, цвет `#15224D` (прозрачность `20%`).
  2. **Левая карточка отрывка:** `89px` × `187px` (Y-смещение `84px` от верха иллюстрации).
  3. **Правая карточка отрывка:** `89px` × `187px` (Y-смещение `84px` от верха иллюстрации).
  4. **Главная центральная карточка:** `85px` × `220px` (Y-смещение `32px`), содержит синюю иконку кавычек `""` (`32px` × `32px`).

---

## 🛈 5. Индикатор свайпа (`Swipe to explore` Node `275:384` & `275:396`)
* **Вертикальный отступ от иллюстрации до стрелки:** `2.5px`.
* **Иконка стрелочки `^` (Node `275:384`):**
  * **Размер:** `13.0px` × `13.0px`.
  * **Цвет:** `#4C5D8B`.
* **Вертикальный отступ от стрелочки до текста:** `11.4px`.
* **Текст `Swipe to explore` (Node `275:396`):**
  * **Позиция Y:** `490.9px` (`618.9px - 128px`).
  * **Размер блока:** `95.0px` × `15.0px`.
  * **Гарнитура:** `Inter`
  * **Начертание:** `400` (Regular)
  * **Размер шрифта:** `12.0px`
  * **Цвет текста:** `#4C5D8B`
  * **Высота строки (Line Height):** `14.5px`

---

## 🔤 6. Главный заголовок (Headline Node `275:407` & `275:408`)
* **Позиция Y общего блока:** `544.9px` (`672.9px - 128px`).
* **Вертикальный отступ от надписи свайпа:** `39.0px`.
* **Строка 1 (`Discover Literature in` Node `275:407`):**
  * **Размеры блока:** `252.0px` × `29.0px`.
  * **Гарнитура:** `Inter` Bold `24.0px`
  * **Цвет:** `#FFFFFF` (чистый белый)
  * **Высота строки:** `29.0px`
* **Строка 2 (`Immersive Passages` Node `275:408`):**
  * **Размеры блока:** `242.0px` × `29.0px`.
  * **Отступ от 1-й строки:** `5.3px`.
  * **Гарнитура:** `Inter` Bold `24.0px`
  * **Цвет:** `#FFFFFF` (чистый белый)
  * **Высота строки:** `29.0px`

---

## 📝 7. Блок описания (Description Body Node `275:419`-`275:421`)
* **Позиция Y:** `617.0px` (`745.0px - 128px`).
* **Вертикальный отступ от заголовка:** `7.7px`.
* **Размер блока:** `312.0px` × `69.0px`.
* **Параметры типографики для всех строк:**
  * **Гарнитура:** `Inter` Regular `14.0px`
  * **Цвет текста:** `#7E90BF`
  * **Высота строки:** `16.9px` (коэффициент `1.21`)
* **Разбиение по строкам из Figma:**
  1. `Scroll through beautifully formatted book ` (`276px` × `17px`)
  2. `passages like short-form content. Each ` (`264px` × `17px`, Y-смещение `20px`)
  3. `passage is 1-2 pages of immersive reading.` (`286px` × `17px`, Y-смещение `40px`)

---

## 🔘 8. Главная кнопка призыва (CTA Button Node `275:424` & `275:432`)
* **Позиция Y:** `704.0px` (`832.0px - 128px`).
* **Вертикальный отступ от описания:** `18.0px`.
* **Размеры кнопки:** Ширина `312.0px`, Высота `56.0px`.
* **Параметры стилизации:**
  * **Цвет фона (Fill):** `#3F63C6` (Solid Blue)
  * **Скругление углов (Border Radius):** `16.0px`
* **Текст на кнопке (`Start Reading` Node `275:432`):**
  * **Размеры блока:** `107.0px` × `19.0px` (выравнивание строго по центру кнопки).
  * **Гарнитура:** `Inter` Bold `16.0px`
  * **Цвет:** `#FFFFFF` (чистый белый)
  * **Высота строки:** `19.4px`

---

## 📌 9. Нижний футер (Footer Node `275:443`)
* **Позиция Y:** `774.9px` (`902.9px - 128px`).
* **Вертикальный отступ от кнопки:** `14.9px`.
* **Размеры блока:** `238.0px` × `15.0px`.
* **Параметры типографики:**
  * **Текст:** `Curated passages from timeless literature`
  * **Гарнитура:** `Inter` Regular `12.0px`
  * **Цвет текста:** `#485984`
  * **Высота строки:** `14.5px`
  * **Выравнивание:** По центру
* **Отступ от футера до нижнего края экрана:** `15.3px`.
